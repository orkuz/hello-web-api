pipeline {
    agent any
    environment {
		DOCKERHUB_CREDENTIALS=credentials('orkuz-dockerhub')
	}
    
    stages {
        stage('Prepare for build') {
            steps {
                echo 'Preparing environment...'
            }
        } 
        
    }
}